spring:
  application:
    name: bulldog
  datasource:
    url: jdbc:postgresql://localhost:15432/bulldog
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
